HOST=0.0.0.0
PORT=3000
SERVICE_NAME=public-http-server

# Set this before exposing /api/* in production.
# Clients must send: Authorization: Bearer <token>
API_TOKEN=change-me

# Optional. Render sets this automatically from the free Postgres database.
# Without it, messages are stored in a local JSONL file.
DATABASE_URL=
