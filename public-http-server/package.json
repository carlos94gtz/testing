{
  "name": "public-http-server",
  "version": "0.1.0",
  "private": true,
  "description": "Small dependency-free HTTP server that can run locally or on an internet-accessible host.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=18"
  }
}
